# Tuples are immutible -cant be changed or modified
# Same as lists but are represented by ()
# Not commonly used compared to lists

coordinate = [(4, 6), (2, 3), (5, 8)]
coordinate[0] = (2, 1)
print(coordinate[0])

